# E1 拿来说明

> 「拿来主义」要点：**拿什么、从哪拿、什么许可、改了什么、我理解了没有。**
> 只写真的拿了的；没拿的部分就写自己写的。

- 学号 / 姓名：`TODO 填你的学号与姓名`
- 项目仓库：`TODO 填仓库地址`
- 日期：2026-09-23

## 一、拿来清单

| # | 拿来的东西 | 来源 | 许可 | 我改了什么 | 我是否理解 |
|---|---|---|---|---|---|
| 1 | CSS 基础重置思路（`box-sizing` 统一、媒体元素块级化、尊重 `prefers-reduced-motion`） | Andy Bell《A Modern CSS Reset》https://piccalil.li/blog/a-modern-css-reset/ | MIT | 只保留本项目需要的最小集：删掉了 `h1~h6 margin:0`、`body min-height:100vh` 等本项目用不到的规则；把"动效尊重系统设置"独立成一条 `@media` 块 | 是——重置的目的是消掉浏览器默认样式差异，让"所见即所写"，所以越少越好，不需要整套框架 |
| 2 | 跳过导航链接（skip-link）写法 | Web 无障碍通行做法（WCAG 2.4.1 Bypass Blocks） | 公共规范做法 | 用 `position:absolute; left:-9999px` + `:focus{left:0}` 实现，聚焦时可见 | 是——让键盘用户不必逐个 Tab 过导航，直接跳到 `#main` |
| 3 | `IntersectionObserver` 做滚动高亮 | MDN IntersectionObserver 文档 | CC-BY-SA（文档） | 只取了 `rootMargin` + `threshold` 的用法，自己写了状态切回逻辑 | 是——`rootMargin` 负值把"判定区"收成一条窄带，进入窄带才算当前区块 |
| 4 | 系统字体栈 | 惯用写法（`-apple-system`, `PingFang SC`, `Microsoft YaHei`…） | 无（字体本身由系统提供） | 按中文优先顺序调整，微软雅黑放最后兜底 | 是——不引外部字体文件，避免首屏闪烁和额外请求 |

## 二、我明确"没拿"的东西

- **没有引入任何 CSS 框架**（Bootstrap / Tailwind）：本项目只有一页，框架体积和心智负担都不划算。
- **没有引入 JS 库**：交互只有导航开合、年份、卡片渲染、滚动高亮，原生 API 足够。
- **没有直接复制任何代码仓库的成品文件**：上表列的 4 项都是"做法/约定"层面，落到本项目的代码是我按自己的结构重写的。

## 三、我对"拿来"的判断标准

1. **能说清它在干什么** —— 说不清就不拿；
2. **许可允许** —— MIT / 公共规范做法可直接用，注明来源；来路不明的一律不用；
3. **改到贴合本页** —— 拿了整套只用一个功能，就只留那一个功能；
4. **注明来源** —— 代码里的注释写清出处，方便别人复核（见 `styles.css` 顶部重置块）。

## 四、遗留 TODO

- [ ] 如果你额外用了图标库、图片素材或配色工具，补充到上表（含链接与许可）
- [ ] 确认仓库地址后回填本文档顶部
