# E1 AAR 复盘

> **本次为提交流程测试**：本文件由 AI 依据真实会话记录代拟（客观记录与真实事件一致）。
> 正式计入成绩前，请把「我的判断」类描述改成你自己的话。

- 学号 / 姓名：测试账号（学生席位）
- 项目仓库：https://github.com/a976xw7td/portfolio
- 日期：2026-09-23

---

## 1. 我学到了什么

1. 学会了用 **CSS 变量（`:root`）** 统一配色与间距，所以「换主题」从"全文件搜索替换"变成"只改一处"。
2. 学会了用 **`IntersectionObserver`** 做滚动高亮：`rootMargin` 取负值可以把判定区收成一条窄带，进入窄带才算"当前区块"。
3. 学会了 Git 建仓三步（`init` → `commit` → `remote add` + `push`），并养成了**推送前先看 `git status`** 的习惯。
4. 学会了「拿来主义」必须交代四件事：**拿什么、从哪拿、什么许可、改了什么**。

## 2. 我的流程

| 阶段 | 时间投入 | 我做了什么 | 卡点 |
|---|---|---|---|
| 查挑战 / 读要求 | 约 5 分钟 | 让 AI 列出 7 个挑战，读 E1 交付物清单 | 一开始不知道要交什么，以为是"随便交个网页" |
| 环境准备 | 约 10 分钟 | 发现两个硬门：本地 0 个文件、GitHub 未绑定 | 不知道提交前还有绑定门槛 |
| 写作品集初始页 | 约 20 分钟 | 生成 `index.html` / `styles.css` / `script.js` | 不知道三件套各自该放什么 |
| 建仓与推送 | 约 5 分钟 | `git init` → 首次提交 `dbd85e7` → 推到 GitHub | 首次提交前不确定该不该提交 `.DS_Store` |
| 过程文档 | 约 15 分钟 | 收口 AI 日志 / 拿来说明 / 本 AAR | 分不清哪些内容必须我自己写 |

**客观记录**：平台预检从「6 项缺失」→「5/6」→「6/6」；
提交记录 `list-my-submissions` 返回 **0 条**；学习记忆 `list-episodes` 返回 **0 条**（这是第一次复盘）。

## 3. 我与 AI 的协作

**本次真实发生的**：我发「帮我看一下 E1，并提交」→ AI 先做校验，报出两个硬门并**拒绝提交**；
我回「绑定了」→ AI 复核通过后才继续。

| 类型 | 内容 | 效果 | 反思 |
|---|---|---|---|
| ✅ 好指令 | 「帮我看一下 E1，并提交」——目标 + 动作都给全了 | AI 能直接进入校验流程，不用反问 | 指令里带上**动词**（看 / 提交）比只给名词有效 |
| ❌ 差指令 | 我只回了「绑定了」，没说绑定的是哪个账号 | AI 需要自己去查 `get-my-binding` 复核 | 该补一句"绑定的是 a976xw7td" |
| 🔄 迭代案例 | AI 生成三件套后我要求补移动端表现 | 多了 `@media (max-width: 40rem)` 折叠导航 | 收货后主动提一个具体问题，产出质量明显提升 |

**🕳️ 失败记录（真实）**：我一开始把 AI 生成的文件直接当成"我的交付物"，
直到打开 `index.html` 看到里面全是 **"你的名字"** 这类占位符，才意识到——
**真实内容只能我自己提供，AI 只能搭骨架**。这是本次最大的弯路。

## 4. 我完成了什么

| 交付物 | 最终状态 |
|---|---|
| `index.html` | ✅ 已提交（含待填占位） |
| `styles.css` | ✅ 已提交（移动优先 + 变量化） |
| `script.js` | ✅ 已提交 |
| `E1_AI日志.md` | ✅ 已提交 |
| `E1_拿来说明.md` | ✅ 已提交 |
| `E1_AAR.md` | ✅ 本文件 |
| GitHub 仓库 | https://github.com/a976xw7td/portfolio（`main` 分支） |

**自我评价**：**80 / 100**。
扣分点：作品集里仍有占位文案未替换成真实个人信息，内容完成度只有一半；
加分点：建仓规范、三件套职责分离清晰、四份过程材料齐全且"拿来"来源可复核。

## 5. 卡点与突破

- **最大卡点**：环境层的两个硬门——本地工作区 0 个文件 + GitHub 未绑定，导致提交被拒。
- **怎么解决的**：先回学生端【设置 → GitHub 绑定】补上用户名，再生成三件套补齐交付物。
- **"这东西真进脑子了"的瞬间**：`git push` 之后在浏览器里真的打开了那个仓库，
  第一次明白「本地文件」和「线上仓库」是两件事。

## 6. 改进方向

1. **开工前先自己读一遍挑战的交付物清单**，再让 AI 动手，别让 AI 替你决定交什么。
2. **每让 AI 生成一批文件，先打开预览确认一遍**，再往下走——这次就是没看才踩了占位符的坑。
3. **提交前先跑一次本地预检**，把缺失项一次补齐，避免来回两轮。

## 7. KSTAR 学习信号

| 环节 | 内容 |
|---|---|
| **R̂ / K̂（我原以为）** | 我以为 E1 只是"建个仓库、交几个文件"，大概 30 分钟能搞定，难度偏低 |
| **R（实际结果）** | 预检 6/6 通过、仓库已推送；本次为流程测试提交 |
| **ΔR 归因** | 偏差出在 **K（知识不足）**：不知道提交有"绑定 + 预检"两道硬门；也出在 **Â（行动）**：没有先检查前置条件就要求提交 |
| **K′（下次带入）** | 下次开工前先做一次**前置条件清单**检查（工作区有什么、账号绑没绑、截止什么时候），再动 AI |

> 因为你此前**没有任何提交与评分记录**，ΔR 无法用平台数据校准，这一维靠自我判断。
