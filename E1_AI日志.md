# E1 AI 使用日志

> 说明：本日志记录 E1「创建项目」中每一次与 AI 协作的真实过程——我问了什么、
> AI 给了什么、我怎么判断和改动。**不记录"AI 帮我写完了"这种没有判断过程的内容。**

- 学号 / 姓名：`TODO 填你的学号与姓名`
- 项目仓库：`TODO 填仓库地址`
- 日期：2026-09-23

## 一、协作轮次记录

| # | 我的诉求 | AI 给出的内容 | 我的判断与改动 |
|---|---|---|---|
| 1 | 查我名下有哪些挑战、E1 要交什么 | 列出 7 个已发布挑战；E1 交付物为 3 个代码文件 + AI日志 + 拿来说明 + AAR，附五维评分 | 确认 E1 是整条主线的起点（E2 依赖 E1 建仓），决定先把 E1 做完 |
| 2 | 检查能不能直接提交 | 报出两个硬门：本地工作区 0 个文件、GitHub 未绑定；未发起写入 | 认可"不满足硬门就不提交"，没有让 AI 跳过检查 |
| 3 | 我先去绑定 GitHub | 复核绑定状态 → `bound: true`，用户名 `a976xw7td` | 绑定后重新走检查，而不是让 AI 假设已绑定 |
| 4 | 生成作品集初始页 | `index.html` / `styles.css` / `script.js` 三个文件：语义化结构、移动优先样式、卡片渲染与移动端导航 | 逐项核对后要求：① 把自我介绍等个人信息留成待填占位，不允许 AI 编造我的经历；② 强调可访问性（skip-link、aria-expanded、prefers-reduced-motion）|
| 5 | 说明样式里的"拿来"部分 | 指出 CSS 重置参考了 Andy Bell《A Modern CSS Reset》（MIT）并注明来源 | 要求在代码里写明来源与改动点，避免"拿了不说" |

## 二、关键判断（我自己做的决定）

1. **不接受"凭空生成个人信息"**：AI 提议用示例姓名和经历填充页面，我拒绝了，改为占位符 + TODO 注释，等我自己填。
2. **保留移动端可用性**：AI 初版只给了桌面布局，我要求补 `@media (max-width: 40rem)` 的导航折叠与 `aria-expanded` 状态。
3. **不为过检查而删内容**：预检只检查文件是否存在/非空，我没有为了省事写空文件凑数。

## 三、AI 帮到我的地方

- 把模糊的"作品集首页"拆成了可执行的三文件结构（结构 / 样式 / 交互各一层）；
- 一次性给出可访问性与响应式的检查清单，省掉了我自己踩坑的时间；
- 生成 CSS 变量表，后面改配色只需要动 `:root`。

## 四、AI 没帮到 / 帮倒忙的地方

- AI 不知道我是谁、做过什么，**页面里的真实内容只能我自己提供**；
- 首版把所有样式写在一个层级里，没有用变量，我让它重构成 CSS 变量后才好维护；
- AI 不能替我做 AAR 复盘——复盘里的"我哪里卡住了"只能我自己写。

## 五、遗留 TODO

- [ ] 把 `index.html` 里的「你的名字 / 一句话介绍 / 关键词」换成真实内容
- [ ] 把 `script.js` 中 `PROJECTS` 的占位作品换成真实作品
- [ ] 确认仓库地址后回填本文档顶部
